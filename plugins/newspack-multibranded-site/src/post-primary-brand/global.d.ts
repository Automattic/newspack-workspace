/**
 * Localized by `Post_Primary_Brand::enqueue_scripts()`
 * (`wp_localize_script( ..., 'newspackPostPrimaryBrandVars', ... )`, see
 * `includes/admin/class-post-primary-brand.php`) on the post edit screen.
 *
 * Global script file: no top-level imports — an import would turn this file
 * into a module and strip the declaration below of its global scope.
 */
declare const newspackPostPrimaryBrandVars: {
	adminURL: string;
	taxonomySlug: string;
	metaKey: string;
	postTypesWithPrimaryBrand: string[];
};
